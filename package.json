{
  "name": "npm-babel-seed",
  "version": "1.0.0",
  "description": "A seed for writing NPM packages with babel",
  "main": "lib/index.js",
  "scripts": {
    "compile": "./node_modules/.bin/babel --presets es2015 -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "npm run compile && echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
